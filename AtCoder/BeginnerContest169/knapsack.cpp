#include <bits/stdc++.h>

std::vector<std::vector<int>> counts;

int main() {
    int N, S;
    std::cin >> N >> S;

    std::vector<int> A(N);
    for (int i = 0; i < N; i++) {
        std::cin >> A[i];
    }

    // counts = std::vector<std::vector<int>>(N, std::vector<int>(N, -1));
}
