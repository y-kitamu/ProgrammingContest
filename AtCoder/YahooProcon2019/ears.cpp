#include <iostream>


int main() {
    // int L;
    // std::cin >> L;

    // int A[L];

    // std::vector<int> block;


    // int cnt_block = 0;
    // int cnt_0 = 0;
    // std::cin >> A[0];
    // cnt += A[0] / 2;
    
    // for (int i = 1; i < L - 1; i++) {
    //     std::cin >> A[i];

    //     if (sum > 0) {
    //         if (A[i] == 0) {
    //             block.push_back(cnt);
    //         } else {
    //             cnt += (A[i] - 1) / 2;
    //         }
    //     }
    // }

    // std::cin >> A[L - 1];
    // cnt += A[L - 1] / 2;
    // block.push_back(cnt);


    
}
