#include <bits/stdc++.h>


int main() {
    int N;
    std::cin >> N;

    std::vector<std::vector<int>> pts(N, std::vector<int>(2));
    for (int i = 0; i < N; i++) {
        std::cin >> pts[i][0] >> pts[i][1];
    }


}
