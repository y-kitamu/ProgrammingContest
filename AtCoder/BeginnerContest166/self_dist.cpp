#include <iostream>
#include <vector>


int main() {
    int N;
    std::cin >> N;

    std::vector<int> A(N);

    std::vector<std::vector<int>> map(N);
    for (int i = 0; i < N; i++) {
        std::cin >> A[i];
    }

    for (int i = 0; i < N; i++) {

    }

}
