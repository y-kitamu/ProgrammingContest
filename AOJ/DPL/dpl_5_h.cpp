#include <bits/stdc++.h>


using ll = long long int;


int main() {
    // ll mod = 1e9 + 7;
    ll n, k;
    std::cin >> n >> k;

    if (n > k) {
        std::cout << 0 << std::endl;
    } else {
        std::cout << 1 << std::endl;
    }
}
